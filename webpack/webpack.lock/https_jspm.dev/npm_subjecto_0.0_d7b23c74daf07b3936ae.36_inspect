import exports$1 from './subject';

var exports = {};

var __importDefault = exports && exports.__importDefault || function (mod) {
  return mod && mod.__esModule ? mod : {
    "default": mod
  };
};

Object.defineProperty(exports, "__esModule", {
  value: true
});

var subject_1 = __importDefault(exports$1);

var lastTime = new Date();
var activated = false;

var inspectStore = function (store, keys, path) {
  if (keys === void 0) {
    keys = [];
  }

  if (path === void 0) {
    path = "";
  }

  if (store) {
    Object.keys(store).forEach(function (key) {
      if (store[key] instanceof subject_1.default && (keys.includes(store[key].name) || keys.length === 0)) {
        store[key].subscribe(function (value) {
          var elapsed = new Date().getTime() - lastTime.getTime();
          console.groupCollapsed("%cstore%c " + path + "." + key + " (%c" + elapsed / 1000 + "s) subs: " + Object.keys(store[key].subscribers).length + " / count: " + store[key].count, "background-color:#000000aa; color: #fff; padding: 2px 4px; border-radius: 3px; margin-right: 4px;font-weight: 400;", "font-weight: 400;", "font-weight: 400;");
          console.log(value);
          console.groupEnd();
          lastTime = new Date();
        });
      } else if (typeof store === "object") {
        inspectStore(store[key], keys, path + "." + key);
      }
    });
  }
};

exports.default = function (store, keys) {
  if (keys === void 0) {
    keys = [];
  }

  if (!activated) {
    inspectStore(store, keys);
  }
};
const __esModule = exports.__esModule;

export { __esModule, exports as default };

//# sourceMappingURL=inspect.map